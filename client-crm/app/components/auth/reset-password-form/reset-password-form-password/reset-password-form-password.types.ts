export interface ResetPasswordFormPasswordProps {
  isLoading: boolean;
  name: string;
  label: string;
  placeholder: string;
  errorMessage?: string;
  isTouched?: boolean;
  passwordValue?: string;
  confirmPasswordValue?: string;
  isConfirmTouched?: boolean;
}

